{
  "name": "modelverse",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pages:dev": "wrangler pages dev dist",
    "deploy": "wrangler pages deploy dist",
    "update-prices": "node scripts/update-prices.mjs",
    "analyze": "node scripts/analyze-models.mjs"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
